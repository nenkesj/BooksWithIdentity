@model HowTo_DBLibrary.Key

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>
<hr />
<h3>Are you sure you want to delete this?</h3>
<form asp-action="Delete">
    <input type="hidden" asp-for="@Model.KeyId" />
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-action="Index">Back to List</a>
</form>
<div>
    <h4>Key</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.KeyId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.KeyId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.KeyText)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.KeyText)
        </dd>
    </dl>
</div>

